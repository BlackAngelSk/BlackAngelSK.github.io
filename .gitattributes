# Auto detect text files and perform LF normalization
* text=auto
#
# Keep Windows batch files byte-exact with CRLF line endings — cmd.exe is
# unreliable with bare-LF .bat files, and these are served for download from
# the site (setup-proxy.bat).
*.bat -text
*.cmd -text
